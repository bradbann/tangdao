<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.tangdao.modules.paas.mapper.PushConfigMapper">
	
	<update id="updateByUserCode">
		update paas_push_config set url = #{url}
		<if test="status != null">
			,status = #{status}
		</if>
		where user_code = #{userCode} and type = #{type}
	</update>
</mapper>